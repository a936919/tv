<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AiSubtitleTonalButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:background">@drawable/selector_button</item>
        <item name="backgroundTint">@null</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>

    <style name="AiSubtitlePrimaryButton" parent="AiSubtitleTonalButton" />

    <style name="AiSubtitleDropdownItem" parent="TextAppearance.Material3.BodyLarge">
        <item name="android:background">@color/ai_dropdown_item_background</item>
        <item name="android:textColor">@color/ai_dropdown_item_text</item>
        <!-- The ListView owns DPAD focus/click; focusable child rows swallow KEYCODE_DPAD_CENTER. -->
        <item name="android:focusable">false</item>
        <item name="android:focusableInTouchMode">false</item>
    </style>
</resources>
